/**
 * Represents a standardized API response.
 * @template T
 * @typedef {object} ApiResponse
 * @property {boolean} success - Indicates if the API call was successful.
 * @property {string | null} message - An optional message from the API.
 * @property {T} data - The generic data payload of the response.
 */

// This empty export allows the JSDoc types to be imported and recognized across your project.
export {};